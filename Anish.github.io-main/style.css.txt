/* Basic Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box; /* Makes layout easier */
}

body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #f4f4f9; /* Light background */
    color: #333;
}

a {
    text-decoration: none;
    color: #007bff; /* Primary link color */
}

/* Navigation Bar */
header {
    background: #333;
    color: white;
    padding: 1em 0;
    position: sticky; /* Keeps it at the top */
    top: 0;
}

nav {
    display: flex; /* Use Flexbox for alignment */
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

nav ul {
    list-style: none;
    display: flex;
}

nav ul li a {
    color: white;
    padding: 10px 15px;
    display: block;
}

/* Projects Grid Example */
#projects {
    padding: 40px 20px;
}

.project-grid {
    display: grid;
    /* This creates a responsive layout: 
       1 column on small screens, and up to 3 on large screens */
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    max-width: 1200px;
    margin: 0 auto;
}

@media (max-width: 768px) {
    /* Stack the navigation items vertically on small screens */
    nav {
        flex-direction: column;
        text-align: center;
    }

    nav ul {
        flex-direction: column;
        width: 100%;
        margin-top: 10px;
    }
}